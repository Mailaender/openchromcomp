<?xml version="1.0" encoding="UTF-8"?>
<project>
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>net.openchrom</groupId>
    <artifactId>net.openchrom.plugins.parent</artifactId>
    <version>BUILD-VERSION</version>
  </parent>

  <name>OpenChrom Compilation Product</name>

  <artifactId>openchrom.compilation</artifactId>
  <packaging>eclipse-repository</packaging>
  <version>0.8.0-SNAPSHOT</version>

  <build>
    <plugins>
      <plugin>
        <groupId>${tycho.groupid}</groupId>
        <artifactId>tycho-p2-publisher-plugin</artifactId>
        <version>${tycho.version}</version>
        <configuration>
          <publishArtifacts>true</publishArtifacts>
        </configuration>
      </plugin>
      <plugin>
        <groupId>${tycho.groupid}</groupId>
        <artifactId>tycho-p2-director-plugin</artifactId>
        <version>${tycho.version}</version>
        <executions>
          <execution>
            <id>materialize-products</id>
            <goals>
             <goal>materialize-products</goal>
            </goals>
          </execution>
          <execution>
            <id>archive-products</id>
            <goals>
              <goal>archive-products</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <products>
            <product>
              <id>net.openchrom.rcp.compilation.product.id</id>
            </product>
          </products>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
